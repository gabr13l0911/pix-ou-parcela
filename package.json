{
  "name": "calculadora-de-descontos",
  "version": "1.0.0",
  "description": "",
  "main": "sw.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:web": "rm -rf www && mkdir www && cp -r index.html manifest.json sw.js css js icons www/",
    "cap:android": "npm run build:web && npx cap sync android",
    "build:android": "npm run cap:android && cd android && ./gradlew assembleDebug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0"
  }
}
